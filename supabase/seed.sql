-- Supabase seed placeholder (non-destructive)
-- This file is intentionally safe: it does not drop or truncate tables.
-- Add INSERT statements guarded with WHERE NOT EXISTS checks as needed.

-- Example (commented):
-- INSERT INTO public.organizations (id, name)
-- SELECT gen_random_uuid(), 'Example Org'
-- WHERE NOT EXISTS (
--   SELECT 1 FROM public.organizations WHERE name = 'Example Org'
-- );
